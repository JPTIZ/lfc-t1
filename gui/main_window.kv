#:import ScrollEffect kivy.effects.scroll.ScrollEffect
<MainWindow>:
    BoxLayout:
        orientation: 'vertical'
        size: root.size
        Panel:
            id: menu_bar
            size: root.size[0], 40
            size_hint: None, None
            StackLayout:
                top: menu_bar.top - 5
                size: self.parent.size
                size_hint: None, None
                Button:
                    text: 'New'
                    on_press: root.new_automata()
                Button:
                    text: 'Load'
                    on_press: root.load_file()
                Button:
                    text: 'Save'
                    on_press: root.save_file()
                VSeparator
                Button:
                    text: 'Add symbol'
                    on_press: root.on_add_symbol()
                Button:
                    text: 'Clear'
                    on_press: root.show_clear_popup()
                Button:
                    id: btn_close
                    text: 'Close'
                    disabled: True
                    on_press: root.close_current()
        FLTabbedPanel:
            id: automata_tabs
